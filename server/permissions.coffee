Actions.deny
  insert: -> Meteor.isClient
  update: -> Meteor.isClient
  remove: -> Meteor.isClient


Events.deny
  insert: -> Meteor.isClient
  update: -> Meteor.isClient
  remove: -> Meteor.isClient
